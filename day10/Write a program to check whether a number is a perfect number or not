# Write a program to check whether a number is a perfect number or not.
def per_num(n):
    a=0
    for i in range(1,n):
        if n%i==0:
            a+=i
        else:
            pass
    return a==n
per_num(495)